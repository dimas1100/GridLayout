package com.example.mygridlayot.model

class AlphaChar {

    var icons:Int ? = 0
    var alpha:String ? = null

    constructor(icons: Int?, alpha: String?) {
        this.icons = icons
        this.alpha = alpha
    }
}
